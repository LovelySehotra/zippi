import { ErrorHandlersFilter } from './error-handlers.filter';

describe('ErrorHandlersFilter', () => {
  it('should be defined', () => {
    expect(new ErrorHandlersFilter()).toBeDefined();
  });
});
